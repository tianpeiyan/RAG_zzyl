分片：文档类型不同、分片方式不同。FAQ按一问一答分、系统手册按章节分#、##

query rewrite：基于关键词匹配，标准化改写。如：怎么入住->入住流程是什么。原始查询+扩展查询一并用于召回

意图识别：寒暄、域外、问题类型（事实、流程、列表...），不同类型对应不同回复规则

检索：混合检索（BM25+向量检索），query rewrite的原始查询和扩展查询分别检索、合并去重、保留最高得分

召回：混合打分、多种奖励机制

重排：百炼的rerank模型、top-k精排、分数融合

片段过滤：必须词覆盖率>=72%；可回答性分数>=0.55；最终分数>=阈值；强关键词/强向量命中/强重排命中至少满足一项；选择上限top-k；

生成：强约束提示词